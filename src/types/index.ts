export interface ISliderItem {
    imgSrc: string;
    id: number;
}

export interface ICategoryMenuItem {
    imgSrc: string;
    id: number;
    title: string;
}

export interface IPizzaDataItem {
    imgSrc: string;
    id: number;
    title: string;
}
